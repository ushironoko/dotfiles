#!/usr/bin/env bash

echo "Never can use Default WebFetch and WebSearch. Can Only use mcp__gemini-google-search or mcp__readability. Use gemini and readability to perform follow-up tasks. Don't ask the user to do it, just keep doing it." 1>&2
exit 2
